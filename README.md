# windows-config
powershell scripts for automatic tiling, minimization and anything else I can reasonably get done in 5 minutes

windows tiling just is kinda gross (slow) but if I need to use windows on a scriptable PC then it should hopefully
